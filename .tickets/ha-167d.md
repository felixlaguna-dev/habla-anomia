---
id: ha-167d
status: closed
deps: [ha-z734]
links: []
created: 2026-07-22T10:13:47Z
type: task
priority: 2
assignee: Félix Laguna Teno
tags: [ux, theme]
---
# Default to light theme for new users (respect OS preference on first run)

BLOCKED BY the theme-token-cleanup ticket (T11) — light and high-contrast themes must be fully coherent before becoming the default. Do not start until it closes.

CONTEXT: the default theme is dark (src/lib/db/settings.ts DEFAULTS.theme = "dark"). For the target audience — elderly, often with reduced contrast sensitivity, using the app in daylight clinics/living rooms — light backgrounds with dark text are the safer, friendlier default, and the light theme (screenshot-verified) already reads better.

TASK:
1. Change DEFAULTS.theme to "light".
2. Existing users must keep whatever theme value is already stored in IndexedDB — verify initDefaults only writes missing keys (it does; leave that behavior intact and note it in the ticket when closing).
3. Respect OS preference on FIRST run only: in initDefaults, when writing the missing theme key, use window.matchMedia("(prefers-color-scheme: dark)") to pick dark for users whose device is dark-mode (browser-only guard). After that the stored value rules.
4. Update the theme-color meta + manifest background_color/theme_color to match the light theme surface (coordinate with the PWA-manifest ticket if it already changed them — check its status first with tk show).
5. Verify: fresh profile (cleared IndexedDB) on a light-mode OS -> app opens light; dark-mode OS -> opens dark; toggling in settings still works live and persists; onboarding preview (if merged) shows the correct initial selection.
CONVENTIONS (apply to all work): Svelte 5 runes only (state/props/derived/effect). All user-facing strings via the t() i18n helper with keys added to ALL 4 locale files (src/lib/i18n/es.json, ca.json, eu.json, en.json). Use CSS variables from src/lib/styles/theme.css, never hardcoded hex. Min 56px touch targets. Exercises are tap-only multiple choice (aphasia patients cannot type). Before closing: npm run build must succeed (and npm run check must not get WORSE than before your change).


## Notes

**2026-07-22T10:17:17Z**

BLOCKED BY ha-z734 (T11). Do not start until it is closed.

**2026-07-29T21:22:03Z**

DONE. Light is the new default; OS dark-mode users get dark on first run; existing users keep their stored preference.

- DEFAULTS.theme changed to 'light' (settings.ts)
- initDefaults checks prefers-color-scheme: dark for first-run theme resolution only
- FOUC eliminated: inline pre-paint script in app.html reads localStorage cache (habla-anomia:theme), falls back to prefers-color-scheme; applyAppearance writes the cache
- Static theme-color meta + manifest theme_color updated to #2563eb (light theme --primary)
- apple-mobile-web-app-status-bar-style → 'default' (correct contrast for light-default app)
- manifest background_color was already #f8fafc (light --bg) from ha-e3bc — confirmed correct

initDefaults only writes missing keys — verified existing users are unaffected. npm run check: 0 errors, 7 warnings (baseline). npm run build: OK.
