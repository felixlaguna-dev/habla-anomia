---
id: ha-5i08
status: closed
deps: [ha-upwu]
links: []
created: 2026-07-22T10:13:47Z
type: feature
priority: 2
assignee: Félix Laguna Teno
tags: [feature, ux, onboarding]
---
# First-run onboarding wizard (language, text size, theme)

BLOCKED BY the language-selector ticket (T13) — onboarding step 1 sets ui_language via its mechanism. Do not start until it closes.

CONTEXT: i18n already contains a full onboarding section (onboarding.* keys in all 4 locales: welcome, choose_language, choose_text_size, lets_start, step N of M) and AppSettings has onboarding_complete (default false) — but no onboarding UI exists. First-run users land on a stats dashboard full of zeros.

TASK — build a first-run wizard at src/routes/onboarding/+page.svelte:
1. Redirect logic in src/routes/+layout.svelte (or +layout.ts): if onboarding_complete is false and current route is not /onboarding, goto base + /onboarding (browser-only; avoid redirect loops; the admin route is exempt).
2. Three steps, one screen each, giant controls, a step indicator using the existing onboarding.step key ("Paso N de M"), and a big "Siguiente" button (min 72px):
   - Step 1 language: the 4 language buttons (same component/pattern the settings selector uses — reuse, do not duplicate), each labeled in its own language. Applies live.
   - Step 2 text size: the 4 sizes as buttons rendered AT their actual size (the Grande button shows visibly larger text) so the choice is self-demonstrating. Applies live using the mechanism from the text-size fix ticket.
   - Step 3 theme: Claro / Oscuro side by side as mini previews (two small mock cards, one light one dark), plus the welcome message (onboarding.welcome_message) and "Empecemos!" finish button.
3. On finish: set onboarding_complete true, goto home.
4. Add a way back in: settings > Acerca de section gets a small "Ver introduccion de nuevo" row that clears the flag and reruns it (i18n x4).
5. No skip-blocking: a "Saltar" text button on every step jumps to finish with defaults (es, normal, light).

VERIFY: clear IndexedDB, reload -> wizard appears; each choice applies immediately and persists; finishing lands on home; wizard never reappears; deep link to /progress while incomplete redirects to onboarding first.
CONVENTIONS (apply to all work): Svelte 5 runes only (state/props/derived/effect). All user-facing strings via the t() i18n helper with keys added to ALL 4 locale files (src/lib/i18n/es.json, ca.json, eu.json, en.json). Use CSS variables from src/lib/styles/theme.css, never hardcoded hex. Min 56px touch targets. Exercises are tap-only multiple choice (aphasia patients cannot type). Before closing: npm run build must succeed (and npm run check must not get WORSE than before your change).


## Notes

**2026-07-22T10:17:17Z**

BLOCKED BY ha-upwu (T13). Do not start until it is closed.
